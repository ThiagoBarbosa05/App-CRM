services:
  app-db:
    image: bitnami/postgresql
    container_name: app-crm-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: app_crm
    ports:
      - "5432:5432"
